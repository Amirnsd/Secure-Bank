<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/updatecustomer.css">
    <title>Update Customer</title>
</head>
<body>
  
    <div class="navbar">
        <h1>Update Customer Information</h1>
        <nav>
            <a href="/" class="btn">Home</a>
            <a href="/saving" class="btn">Savings Account</a>
            <a href="/chequing" class="btn">Chequing Account</a>
        </nav>
    </div>

    
    <div class="container">
        <h2>Update Information for Customer: <%= person.Name %></h2>
        
        <form action="/updatecustomer/<%= person.AccountNumber %>" method="POST">
            <div class="form-group">
                <label for="name">Name</label>
                <input type="text" name="Name" id="name" value="<%= person.Name %>" required>
            </div>
            <div class="form-group">
                <label for="phoneNumber">Phone Number</label>
                <input type="text" name="PhoneNumber" id="phoneNumber" value="<%= person.PhoneNumber %>" required>
            </div>
            <div class="form-group">
                <label for="streetName">Street Name</label>
                <input type="text" name="StreetName" id="streetName" value="<%= person.StreetName %>" required>
            </div>
            <div class="form-group">
                <label for="city">City</label>
                <input type="text" name="City" id="city" value="<%= person.City %>" required>
            </div>
            <div class="form-group">
                <label for="province">Province</label>
                <input type="text" name="Province" id="province" value="<%= person.Province %>" required>
            </div>
            <div class="form-group">
                <label for="postalcode">Postal Code</label>
                <input type="text" name="Postalcode" id="postalcode" value="<%= person.Postalcode %>" required>
            </div>
            <div class="form-group">
                <label for="email">Email</label>
                <input type="email" name="Email" id="email" value="<%= person.Email %>" required>
            </div>
            <div class="form-group">
                <label for="chequingBalance">Chequing Account Balance ($)</label>
                <input type="number" name="chequingBalance" id="chequingBalance" value="<%= Number(person.chequingBalance).toFixed(2) %>" step="0.01" min="0">
            </div>
            <div class="form-group">
                <label for="savingsBalance">Savings Account Balance ($)</label>
                <input type="number" name="savingsBalance" id="savingsBalance" value="<%= Number(person.savingsBalance).toFixed(2) %>" step="0.01" min="0">
            </div>
            <button type="submit" class="update-btn">Update</button>
        </form>
    </div>

   
    <div class="footer">
        <p>&copy; 2024 Customers Information System</p>
    </div>
</body>
</html>